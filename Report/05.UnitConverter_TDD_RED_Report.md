# UnitConverter_18 — TDD RED Command · Harness 복원 · RED 테스트

| 항목 | 내용 |
|:---|:---|
| 프로젝트 | `UnitConverter_18` |
| 단계 | Phase A (세션 3) — Test Loop RED |
| 보고서 생성일 | 2026-06-11 |
| 목적 | `/tdd-red` Command 정립, Harness 골격 복원, RED-1~3 실패 확인 |
| 관련 Transcript | [`Prompting/05.UnitConverter_TDD_RED_Export-Transcript.md`](../Prompting/05.UnitConverter_TDD_RED_Export-Transcript.md) |

---

## 1. 요약

| 구분 | 결과 |
|:---|:---|
| Command | `.cursor/commands/tdd-red.md` 신규 — Phase 선언, AAA, pytest 예시, 보고 형식, 금지 |
| TDD 순서 이슈 | Harness 이후 Phase A가 RED 없이 GREEN까지 진행되어 첫 `/tdd-red`는 **4 passed** |
| Harness 복원 | `converter/`·`tests/`·`UnitConverter.py` 골격(`...`)으로 되돌림 |
| RED 재실행 | `/tdd-red` — `tests/test_red.py`만 수정, **4 failed** *(RED 성공)* |
| **판정** | **RED 단계 완료** — GREEN(`converter/` 구현) 대기 |

---

## 2. 핵심 결정·산출물

### 2.1 `/tdd-red` Command

| 섹션 | 내용 |
|:---|:---|
| Phase 선언 | `Phase: RED \| Scope: Phase A (세션 3)` |
| AAA | Arrange → Act → Assert, 엄격 `==` |
| RED 시나리오 | RED-1 (`meter:2.5`), RED-2 (`cross_check`), RED-3 (tamper 회귀) |
| 금지 | `converter/`·`src/`·`UnitConverter.py` 수정, assert 완화, skip/xfail |
| SSOT | `.cursorrules`, `docs/PRD.md` §7 |

### 2.2 TDD 순서 교정

**문제:** Harness 생성 시 “골격만” 요청했으나, 이후 “Phase A 진행”에서 구현이 먼저 채워져 RED가 pass로 끝남.

**조치:**

```
골격 Harness → (RED 생략·GREEN 선행) → Harness 복원 → /tdd-red → 4 failed
```

| 시점 | `converter/` | `tests/test_red.py` | pytest |
|:---|:---|:---|:---|
| Harness 최초 | `...` 스텁 | `test_red1/2` 스텁 | 2 passed (빈 본문) |
| Phase A 일괄 구현 | 전체 구현 | RED-1~3 assert | 4 passed |
| Harness 복원 | `...` 스텁 | `test_red1/2` 스텁 | 2 passed |
| `/tdd-red` (본 세션) | 미변경 | RED-1~3 AAA assert | **4 failed** |

### 2.3 RED 테스트 (`tests/test_red.py`)

| Test ID | Given | Assert |
|:---|:---|:---|
| **RED-1** | `meter:2.5` | `feet==8.2`, `yard==2.7` |
| **RED-2** | `feet:8.2` vs `meter:2.5` | `cross_check is True` |
| **RED-3** (ratio) | `M_TO_FT` tamper | `_assert_red1` → `AssertionError` |
| **RED-3** (rounding) | `DECIMAL_PLACES` tamper | `_assert_red1` → `AssertionError` |

### 2.4 pytest (RED 완료 시점)

```bash
python -m pytest tests/test_red.py -v
```

| 테스트 | 실패 원인 |
|:---|:---|
| RED-1 | `run_skill` 스켈레톤 → `None` (`TypeError`) |
| RED-2 | `cross_check` 스켈레톤 → `None` |
| RED-3 (×2) | `converter.commands`에 `M_TO_FT`/`DECIMAL_PLACES` 없음 (`AttributeError`) |

**결과:** 4 failed, 0 passed

### 2.5 생성·변경 파일

| 파일 | 변경 |
|:---|:---|
| `.cursor/commands/tdd-red.md` | 신규 |
| `converter/constants.py` | 골격 복원 (docstring만) |
| `converter/commands.py` | 골격 복원 (`...`) |
| `converter/skill.py` | 골격 복원 (`...`) |
| `tests/test_red.py` | RED-1~3 assert + AAA |
| `UnitConverter.py` | 골격 복원 (`main()` `...`) |

---

## 3. 다음 단계

| 순서 | 작업 | 대상 |
|:---:|:---|:---|
| 1 | **GREEN** | `converter/constants.py` → `commands.py` → `skill.py` 최소 구현 |
| 2 | pytest | RED-1·RED-2 **pass**, RED-3 tamper 시 `AssertionError` 확인 |
| 3 | CLI | `UnitConverter.py` → `run_skill` 연결 |
| 4 | Phase A Done | PRD §9 체크리스트 확인 |

---

*본 문서는 Report/05.UnitConverter_TDD_RED_Report.md — TDD RED Command · Harness 복원 · RED 테스트 세션 보고서입니다.*
